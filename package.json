{
  "name": "health-platform-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "serve -s build",
    "build": "react-scripts build"
  },
  "dependencies": {
    "serve": "^14.0.1",
    "react-scripts": "latest"
  }
}