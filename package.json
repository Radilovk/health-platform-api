{
  "name": "health-platform-frontend",
  "version": "1.0.0",
  "description": "A health platform for users dashboard progress and performance tracking.",
  "main": "index.js",
  "scripts": {
    "start": "node -r react-scripts start /src/index.js",
    "build": "react-scripts build",
    "test": "echo 'Finding tests' && exit 0",
    "deploy": "git push heroku main",
    "postbuild": "git push to heroku main"
  },
  "dependencies": {
    "react": "^16.13.0",
    "react-dom": "^16.13.0",
    "react-scripts": "^4.0.3",
    "babel-preset-react-app": "^4.0.0",
    "@babel/plugin-proposal-private-property-in-object": "^7.12.0",
    "cors": "^2.13.0"
  },
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  }
}
